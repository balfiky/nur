data_dir: data
max_queue_per_user: 3
max_active_sessions: 10
session_timeout_seconds: 1800

# Telegram (leave token empty to disable)
telegram_token: ""
telegram_allowlist: []   # numeric user IDs; empty = allow all
telegram_poll_timeout: 30
dedupe_ttl: 60
